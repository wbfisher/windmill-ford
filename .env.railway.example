# Example Railway Environment Variables
# DO NOT commit actual credentials to git!

# ===================================
# REQUIRED VARIABLES
# ===================================

# Database connection - Railway will auto-inject this
# In Railway Variables tab, use: ${{windmill-db.DATABASE_URL}}
DATABASE_URL=${{windmill-db.DATABASE_URL}}

# Windmill mode
MODE=standalone

# Base URL - Railway will auto-inject this
# In Railway Variables tab, use: ${{RAILWAY_PUBLIC_DOMAIN}}
WM_BASE_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}

# ===================================
# OPTIONAL VARIABLES
# ===================================

# Disable secure cookies (useful for testing)
DISABLE_SECURE_COOKIES=true

# Fleet database connection (for Ford Pro integration)
# In Railway Variables tab, use: ${{fleet-db.DATABASE_URL}}
FLEET_DATABASE_URL=${{fleet-db.DATABASE_URL}}

# ===================================
# FORD PRO API CREDENTIALS (OPTIONAL)
# ===================================

# Ford Pro Client ID
FORD_PRO_CLIENT_ID=your_client_id_here

# Ford Pro Client Secret
FORD_PRO_CLIENT_SECRET=your_client_secret_here

# Ford Pro Fleet ID
FORD_PRO_FLEET_ID=your_fleet_id_here

# ===================================
# NOTES
# ===================================

# 1. Copy these variable NAMES to Railway Variables tab
# 2. For ${{service.VARIABLE}} syntax:
#    - Use this EXACT syntax in Railway (don't replace with actual values)
#    - Railway will automatically inject the referenced service's variables
# 3. For actual values (like FORD_PRO_CLIENT_ID):
#    - Replace "your_client_id_here" with your actual value
# 4. Never commit real credentials to git
